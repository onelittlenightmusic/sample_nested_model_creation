<%= simple_form_for(@descendant) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :child %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
