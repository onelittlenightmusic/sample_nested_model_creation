<%= simple_form_for(@brother) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :parent %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
